#include <stdio.h>
int main()
{
    int x = 1;
    if (x = 5) // the reason it printed here because here the data is being reassigned  to x variable and if the x=0 it will show as not equal
    {
        printf("The number is equal\n");
    }
    else
    {
        printf("The number is equal\n");
    }

    return 0;
}