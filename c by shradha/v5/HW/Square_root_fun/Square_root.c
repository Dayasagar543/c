#include <stdio.h>
float sq_root(int value);
int main()
{
    int number;
    printf("please enter the value you want to find the square root of : \t");
    scanf("%d", &number);
    sq_root(number);

    return 0;
}
float sq_root(int value)
{
    
}